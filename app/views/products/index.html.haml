%h1 商品一覧


.card-deck
  - @products.each do |product|
    .card{style: "width: 20rem;"}
      - if product.image.attached?
        %img.card-img-top{ src: url_for(product.image) }
      .card-body
        %h4.card-title #{product.name} #{product.price}
        %p.card-text
          = product.description
      .card-footer
        %p.card-text
          = link_to '詳細', product, class: "btn btn-primary"
          = link_to '編集', edit_product_path(product), class: "btn btn-default"
          = link_to '削除', product, method: :delete, data: { confirm: 'よろしいですか？' }, class: "btn btn-danger"
%br

= link_to '新規商品', new_product_path
